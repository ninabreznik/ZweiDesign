<%= content_tag_for(:div, conversation) do |conversation| %>
      <i class="fa fa-comment-o"></i> <%= link_to conversation.subject, conversation_path(conversation), target: :chat_window %>
    <br> <%= conversation.updated_at.strftime("%d.%m. ob %H:%M") %>
<% end %>
